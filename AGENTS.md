# AGENTS.md（グローバル共通ガイドライン）

すべてのプロジェクトに共通する開発ガイドライン。
プロジェクト固有の AGENTS.md / CLAUDE.md がある場合は、そちらの記述を優先する。

## コミュニケーション・言語

- **人間の目を通すものはすべて日本語で書く。** ユーザーへの質問・回答、planの出力、
  テスト名（describe/it/test の説明文）、コミットメッセージ、ドキュメントなどが対象。
  エージェント内部の思考や作業（コード、変数名、ツール操作など）では英語を使ってよい
- プロジェクトの慣習が英語の場合はそれに従う
- 仕様が不明確な場合は推測で進めず、確認してから実装する

## 新しくプロジェクトを開始する際の規約

プロジェクト開始時に、リンター・フォーマッター・ユニットテストツールを導入する。

| 言語 | リンター | フォーマッター | ユニットテスト |
| --- | --- | --- | --- |
| TypeScript | ESLint | Prettier | Vitest |
| Python | pylint | black | pytest |

- フロントエンドのプロジェクトでは、Playwright で E2E テストを行う
- フロントエンドを React で書く場合、特に指定がなければ UI ライブラリとして MUI (Material UI) を導入する
- **format on save を有効化する。** `.vscode/settings.json` をリポジトリに含め、
  `editor.formatOnSave: true` と言語ごとの `editor.defaultFormatter`
  （TypeScript: Prettier、Python: black）を設定する

## 開発時のルール

- **TDD で進める。** ユニットテストを先に書いてから実装に入る
- **責務分離を意識する。** クラス・関数・コンポーネントそれぞれが単一の責務を持つように設計し、
  肥大化したら分割する

## 設計原則

- **YAGNI**: 要求されていない機能の先回り実装や、過剰な抽象化・カスタマイズをしない。
  まず最小限で動くシンプルなものを作る
- **DRY**: 同じ知識・ロジックの重複を避け、共通化する。ただし偶然似ているだけのコードを
  無理にまとめない
- **SOLID**: 特に単一責任の原則と、インターフェースを介した疎結合（依存性逆転）を意識する

## コーディング全般

- 既存コードのスタイル・命名・イディオムに合わせる
- テストは実装の詳細ではなく、外から見える振る舞いに対して書く
- リンター・フォーマッター・テストをパスした状態でコミットする
- **ドキュメントを同期する。** 挙動や構成を変えたら、README・仕様書などの関連ドキュメントも
  同じ変更内で更新する
- **設計ドキュメントは SSOT（Single Source of Truth）を意識する。** 同じ内容を複数の箇所に
  繰り返して記載しない。各情報の記載場所を 1 か所に定め、他の箇所からは参照（リンク）で示す

## コミット運用ルール（レビューしやすい粒度でのコミット）

- 1コミット = 1つの論理的な変更にすること。複数の目的（機能追加・リファクタ・設定変更など）を混在させない。
- 変更ファイル数は、1コミットあたり10ファイル程度までを目安に、大きくなりすぎる場合は意味のある単位で分割する。
  - 例: 設定変更と実装コードは分ける／リファクタと新機能追加は分ける／整形のみの変更は別コミットにする
- コミットは作成してよいが、**リモートへの `push` は行わない**。ローカルコミットの状態でユーザーに提示し、push はユーザー自身が行う。
