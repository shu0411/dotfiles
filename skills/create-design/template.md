# design.md テンプレート

`docs/design.md` は必ずこの章立てで作成する。`<!-- -->` のコメントは記入時の指示で
あり、生成する design.md には含めない。該当する内容がない節は「なし」と明記する
（節ごと削除しない）。

```markdown
# Design

- Source: docs/concept.md
- Status: Functional Specification Draft
- Last updated: YYYY-MM-DD

## 1. 仕様設計

### 1.1 対象範囲

#### 対象

<!-- MVP で作るもの。箇条書き -->

#### 対象外

<!-- 今回は作らないと明示するもの。concept.md の「MVP の対象外」を反映 -->

### 1.2 想定ユーザーと利用場面

### 1.3 機能概要

<!-- 次の 3 点を書く。
     1. アプリ全体が何をするものかの概要（1〜2 文）
     2. アプリ全体の入力・出力（外部との境界。取り込むデータ、書き出すデータ、
        保存先など。個々の操作の入出力は 1.5 が記載場所なのでここには書かない）
     3. 機能の一覧（機能名のみ、1 機能 1 行。詳細は 1.5 参照） -->

### 1.4 画面・操作フロー

<!-- 画面（状態）の一覧と遷移を、機能名のレベルで書く。テキストまたは簡単な図。
     機能をまたぐ利用の流れの唯一の記載場所。
     各操作の入力・出力・エラーはここに書かず 1.5 を参照する（SSOT） -->

### 1.5 機能仕様

<!-- 機能ごとに「#### 機能名」の節を作り、以下の項目を太字ラベルの箇条書きで書く。
     複数機能に共通する振る舞いはここに書かず 1.6 に書く（SSOT） -->

#### 機能名

- **入力**:
- **正常系の振る舞い**:
- **出力**:
- **エラー・空状態**:
- **受け入れ条件**:
  <!-- テストへ変換できる具体的な表現で書く。
       例: 「メンバーが 0 人のとき、追加ボタンを押すと、1 人目が中央に配置される」 -->

### 1.6 機能間の共通ルール

<!-- バリデーション、表示形式、共通のエラー挙動など、複数機能にまたがるルールの
     唯一の記載場所。各機能仕様からはここを参照する -->

### 1.7 未決事項

<!-- ユーザーの判断が必要な項目。決まったら本文へ移し、ここから削除する -->

## 2. 技術設計

<!-- Status が Functional Specification Confirmed になるまで作成しない。
     初回作成時はこの章自体（この見出しを含めて）を design.md に含めない。
     仕様設計に書かれた内容は再掲せず、章番号で参照する（SSOT） -->

### 2.1 システム構成と責務分割

### 2.2 データ・状態設計

### 2.3 処理・データフロー

### 2.4 エラー処理

### 2.5 テスト方針

<!-- 1.5 の受け入れ条件と 1 対 1 で対応させる。TDD で先に書くテストを明示する -->

### 2.6 実装順序

### 2.7 技術的リスク・未決事項
```
